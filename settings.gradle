pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

include ':app'

include ':opencv'
project(':opencv').projectDir = new File(opencvsdk + '/sdk')
